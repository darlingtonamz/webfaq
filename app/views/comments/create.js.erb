$('#comment_form_<%= @comment.question_id %>').remove();
$('#comment_but_<%= @comment.question_id %>').show();
$('#comment_count_<%= @comment.question_id %>').empty()
		.append('<%= link_to "#{@comment.question.comments.size} comments", 
			comments_path(question_id: @comment.question_id), 
			 remote: true %>')