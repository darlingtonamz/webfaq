<%= form_for @faq, :url => {:action => 'create'}, remote: true, html:{id: 'new_task'} do |f|%>
<% if @faq.nil? && @faq.error.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@faq.errors.count, "error") %> prohibited this content from being saved:</h2>
    <ul>
      <% @faq.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
	<%= f.text_field :title, placeholder: 'FAQ title' %> 
	<%= f.submit "Submit" , class: 'btn btn-primary'%>
<% end %>

<!=, remote: true=>